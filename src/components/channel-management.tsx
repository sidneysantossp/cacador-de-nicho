'use client';

import { useMemo, useState } from 'react';
import { BrainCircuit, Check, CircleDot, Film, FolderKanban, Layers3, Plus, Sparkles } from 'lucide-react';
import type { AutopilotReadiness, Channel, ChannelBrain, ManagedChannel, NextEpisodePlan, Opportunity } from '@/lib/types';
import { autopilotDecisionPreview, defaultChannelAutopilot, effectiveChannelAutopilot } from '@/lib/channel-autopilot-policy';

const stages:Record<ManagedChannel['stage'],string>={idea:'Ideia',research:'Em pesquisa',production:'Em produção',published:'Publicado',paused:'Pausado'};
const priorities:Record<ManagedChannel['priority'],string>={high:'Alta prioridade',normal:'Prioridade normal',low:'Baixa prioridade'};
const autopilotIssueLabels:Record<string,string>={
 'autopilot-disabled':'Autopilot está desligado.',
 'learning-loop-disabled':'Learning Loop automático está desligado.',
 'auto-plan-disabled':'Geração automática do próximo plano está desligada.',
 'autonomous-mode-required':'Autoaceite exige modo Autonomous.',
 'auto-accept-disabled':'Autoaceite do próximo episódio está desligado.',
 'recommended-candidate-missing':'O plano não possui candidato recomendado válido.',
 'narrative-not-ready':'O candidato recomendado não está narrativamente pronto.',
 'narrative-blockers':'A Narrative Intelligence encontrou blockers.',
 'evidence-below-threshold':'A evidência está abaixo do threshold configurado.',
 'no-strong-learning-evidence':'Falta learning de confiança média/alta sustentando a recomendação.',
 'auto-accept-cooldown':'Já houve um autoaceite neste canal nas últimas 24h.',
 'episode-automation-active':'Já existe uma Episode Automation ativa neste canal.'
};
type Draft=Omit<ManagedChannel,'id'|'createdAt'|'updatedAt'>;
const empty=():Draft=>({name:'',niche:'Finanças',format:'2D animation',stage:'idea',priority:'normal',description:'',autopilot:{...defaultChannelAutopilot}});

export default function ChannelManagement({items,brains,radarChannels,demo,onSave,onMessage,onOpenBrain}:{items:ManagedChannel[];brains:ChannelBrain[];radarChannels:Channel[];demo:boolean;onSave:(channel:ManagedChannel)=>void;onMessage:(message:string)=>void;onOpenBrain:(channel:ManagedChannel)=>void}){
 const [draft,setDraft]=useState<Draft>(empty),[showForm,setShowForm]=useState(false),[filter,setFilter]=useState<'all'|ManagedChannel['stage']>('all');
 const [previewPlans,setPreviewPlans]=useState<Record<string,NextEpisodePlan|null>>({});
 const [previewOperationalIssues,setPreviewOperationalIssues]=useState<Record<string,string[]>>({});
 const [previewLoaded,setPreviewLoaded]=useState<Record<string,boolean>>({});
 const [previewBusy,setPreviewBusy]=useState('');
 const [readinessByChannel,setReadinessByChannel]=useState<Record<string,AutopilotReadiness>>({});
 const [readinessBusy,setReadinessBusy]=useState('');
 const visible=useMemo(()=>filter==='all'?items:items.filter(item=>item.stage===filter),[filter,items]);
 const brainByChannel=useMemo(()=>new Map(brains.map(brain=>[brain.channelId,brain])),[brains]);
 const opportunities=radarChannels.flatMap(channel=>(channel.analysis?.opportunities??[]).map(opportunity=>({channel,opportunity})));
 function save(event:React.FormEvent){event.preventDefault();if(!draft.name.trim()){onMessage('Dê um nome ao canal antes de salvar.');return;}const now=new Date().toISOString();onSave({id:crypto.randomUUID(),...draft,name:draft.name.trim(),description:draft.description.trim(),createdAt:now,updatedAt:now});setDraft(empty());setShowForm(false);}
 function addFromOpportunity(channel:Channel,opportunity:Opportunity){const now=new Date().toISOString();onSave({id:crypto.randomUUID(),name:opportunity.name,niche:channel.niche,format:channel.format.split(' · ')[1]??channel.format,stage:'idea',priority:'normal',description:opportunity.lens,sourceChannelId:channel.id,opportunityId:opportunity.id,createdAt:now,updatedAt:now});}
 async function loadAutopilotPreview(channelId:string){
  setPreviewBusy(channelId);
  try{
   const response=await fetch('/api/next-episode?channelId='+encodeURIComponent(channelId),{cache:'no-store'});
   const body=await response.json().catch(()=>({}));
   if(!response.ok)throw new Error(body.message??'Falha ao simular a decisão do Autopilot.');
   setPreviewPlans(prev=>({...prev,[channelId]:(body.activePlan??null) as NextEpisodePlan|null}));
   setPreviewOperationalIssues(prev=>({...prev,[channelId]:Array.isArray(body.operationalIssues)?body.operationalIssues:[]}));
   setPreviewLoaded(prev=>({...prev,[channelId]:true}));
  }catch(error){
   onMessage(error instanceof Error?error.message:'Falha ao simular a decisão do Autopilot.');
  }finally{setPreviewBusy('');}
 }
 async function loadAutopilotReadiness(channelId:string){
  setReadinessBusy(channelId);
  try{
   const response=await fetch('/api/autopilot-readiness?channelId='+encodeURIComponent(channelId),{cache:'no-store'});
   const body=await response.json().catch(()=>({}));
   if(!response.ok)throw new Error(body.message??'Falha ao checar readiness do Autopilot.');
   setReadinessByChannel(prev=>({...prev,[channelId]:body as AutopilotReadiness}));
  }catch(error){
   onMessage(error instanceof Error?error.message:'Falha ao checar readiness do Autopilot.');
  }finally{setReadinessBusy('');}
 }
 return <>
  <section className="channel-management-hero"><div><div className="eyebrow">PORTFÓLIO EDITORIAL</div><h2>Seus canais.<br/><span>Uma operação com foco.</span></h2><p>Transforme descobertas em canais ativos, com contexto, estágio e próxima prioridade visíveis.</p></div><div className="portfolio-metrics"><div><span>EM PRODUÇÃO</span><strong>{items.filter(item=>item.stage==='production').length}</strong></div><div><span>EM PESQUISA</span><strong>{items.filter(item=>item.stage==='research').length}</strong></div><div><span>PUBLICADOS</span><strong>{items.filter(item=>item.stage==='published').length}</strong></div></div></section>
  <section className="channel-management-toolbar"><div className="segmented" aria-label="Filtrar canais da operação">{([['all','Todos'],['idea','Ideias'],['research','Pesquisa'],['production','Produção'],['published','Publicados']] as const).map(([id,label])=><button key={id} className={filter===id?'selected':''} onClick={()=>setFilter(id)}>{label}</button>)}</div><button className="button primary" onClick={()=>setShowForm(!showForm)}><Plus size={17}/>{showForm?'Fechar cadastro':'Novo canal'}</button></section>
  {showForm&&<section className="panel channel-form"><div className="panel-heading"><FolderKanban size={20}/><h2>Cadastrar canal</h2></div><form onSubmit={save}><div className="form-grid"><label>Nome do canal <small>Em inglês, pois é o nome voltado ao público.</small><input autoFocus value={draft.name} maxLength={100} onChange={event=>setDraft({...draft,name:event.target.value})} placeholder="Ex.: The First Economy"/></label><label>Nicho<input value={draft.niche} maxLength={80} onChange={event=>setDraft({...draft,niche:event.target.value})}/></label><label>Formato<select value={draft.format} onChange={event=>setDraft({...draft,format:event.target.value})}><option>2D animation</option><option>3D animation</option><option>Long form</option><option>Shorts</option><option>Documentary</option></select></label><label>Estágio<select value={draft.stage} onChange={event=>setDraft({...draft,stage:event.target.value as ManagedChannel['stage']})}>{Object.entries(stages).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label>Prioridade<select value={draft.priority} onChange={event=>setDraft({...draft,priority:event.target.value as ManagedChannel['priority']})}>{Object.entries(priorities).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label></div><label>Proposta e contexto<textarea rows={3} maxLength={1000} value={draft.description} onChange={event=>setDraft({...draft,description:event.target.value})} placeholder="Qual é a promessa editorial deste canal?"/></label><button className="button primary"><Check size={17}/>Adicionar ao portfólio</button></form></section>}
  <section className="managed-channel-grid">{visible.map(item=>{const brain=brainByChannel.get(item.id);return <article className="managed-channel-card" key={item.id}><div className="managed-card-top"><span className={`managed-icon ${item.stage}`}><Film size={20}/></span><select aria-label={`Estágio de ${item.name}`} value={item.stage} onChange={event=>onSave({...item,stage:event.target.value as ManagedChannel['stage'],updatedAt:new Date().toISOString()})}>{Object.entries(stages).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></div><div><h2>{item.name}</h2><p>{item.description||'Contexto editorial ainda não registrado.'}</p></div><div className="managed-meta"><span>{item.niche}</span><span>{item.format}</span>{brain&&<span className="brain-version-chip">Brain v{brain.version}</span>}</div><div className="managed-autopilot">{(()=>{const autopilot=effectiveChannelAutopilot(item);return <><div><span>AUTOPILOT</span><select aria-label={'Autopilot de '+item.name} value={autopilot.enabled?autopilot.mode:'off'} onChange={event=>{const value=event.target.value;onSave({...item,autopilot:value==='off'?{...autopilot,enabled:false}:{...autopilot,enabled:true,mode:value as 'assisted'|'autonomous'},updatedAt:new Date().toISOString()});}}><option value="off">Desligado</option><option value="assisted">Assisted</option><option value="autonomous">Autonomous</option></select></div><label><input type="checkbox" disabled={!autopilot.enabled} checked={autopilot.startOnAcceptedNextEpisode} onChange={event=>onSave({...item,autopilot:{...autopilot,startOnAcceptedNextEpisode:event.target.checked},updatedAt:new Date().toISOString()})}/><span>Iniciar ao aceitar Next Episode</span></label><label><input type="checkbox" disabled={!autopilot.enabled} checked={autopilot.learningLoopEnabled} onChange={event=>onSave({...item,autopilot:{...autopilot,learningLoopEnabled:event.target.checked},updatedAt:new Date().toISOString()})}/><span>Aprender automaticamente após publicar · 24h / 72h / 7d</span></label><label><input type="checkbox" disabled={!autopilot.enabled||!autopilot.learningLoopEnabled} checked={autopilot.autoAnalyzeAudience} onChange={event=>onSave({...item,autopilot:{...autopilot,autoAnalyzeAudience:event.target.checked},updatedAt:new Date().toISOString()})}/><span>Analisar comentários e sentimento</span></label><label><input type="checkbox" disabled={!autopilot.enabled||!autopilot.learningLoopEnabled} checked={autopilot.autoPlanNextEpisode} onChange={event=>onSave({...item,autopilot:{...autopilot,autoPlanNextEpisode:event.target.checked},updatedAt:new Date().toISOString()})}/><span>Gerar automaticamente o próximo plano</span></label><div className="managed-autopilot-next"><label><span>Janela da decisão</span><select disabled={!autopilot.enabled||!autopilot.autoPlanNextEpisode} value={autopilot.nextEpisodeTriggerHours} onChange={event=>onSave({...item,autopilot:{...autopilot,nextEpisodeTriggerHours:Number(event.target.value)},updatedAt:new Date().toISOString()})}><option value={24}>24h</option><option value={72}>72h</option><option value={168}>7 dias</option></select></label><label><span>Evidência mínima</span><select disabled={!autopilot.enabled||!autopilot.autoPlanNextEpisode} value={autopilot.nextEpisodeMinEvidence} onChange={event=>onSave({...item,autopilot:{...autopilot,nextEpisodeMinEvidence:event.target.value as 'medium'|'high'},updatedAt:new Date().toISOString()})}><option value="high">Alta</option><option value="medium">Média</option></select></label></div><label><input type="checkbox" disabled={!autopilot.enabled||autopilot.mode!=='autonomous'||!autopilot.autoPlanNextEpisode} checked={autopilot.autoAcceptNextEpisode} onChange={event=>onSave({...item,autopilot:{...autopilot,autoAcceptNextEpisode:event.target.checked},updatedAt:new Date().toISOString()})}/><span>Aceitar recomendação automaticamente quando os gates passarem</span></label><button type="button" className="button subtle small managed-autopilot-preview-button" disabled={previewBusy===item.id} onClick={()=>void loadAutopilotPreview(item.id)}>{previewBusy===item.id?'Simulando…':'Simular decisão'}</button>{previewLoaded[item.id]&&(()=>{const base=autopilotDecisionPreview(item,previewPlans[item.id]??null);const canary=previewOperationalIssues[item.id]??[];const preview=base.action==='auto-accept'&&canary.length?{...base,action:'review' as const,issues:[...base.issues,...canary]}:base;return <div className={'managed-autopilot-preview '+preview.action}><div><strong>{preview.action==='auto-accept'?'Aceitaria automaticamente':preview.action==='review'?'Mandaria para revisão':preview.action==='generate-plan'?'Geraria um novo plano':'Não executaria'}</strong><span>{preview.triggerWindowHours?('Janela: '+preview.triggerWindowHours+'h'):'Sem janela ativa'}{preview.evidenceStrength?' · evidência '+preview.evidenceStrength:''}</span></div>{preview.candidateTitle&&<p>Candidato: {preview.candidateTitle}</p>}{preview.action==='generate-plan'&&<p>Ainda não existe plano ativo nesta versão do Brain. O Autopilot geraria um na janela configurada.</p>}{preview.issues.length>0&&<ul>{preview.issues.map(issue=><li key={issue}>{autopilotIssueLabels[issue]??issue}</li>)}</ul>}{preview.action==='auto-accept'&&<p>Todos os gates atuais passaram. Em modo real, este candidato seria aceito e seguiria para Episode Automation.</p>}</div>})()}</>;})()}</div><div className="managed-card-footer"><span className={`priority priority-${item.priority}`}><CircleDot size={13}/>{priorities[item.priority]}</span><span>{item.opportunityId?'Vinculado a uma oportunidade':'Canal independente'}</span></div><button className="managed-brain-button" onClick={()=>onOpenBrain(item)}><BrainCircuit size={16}/><span>{brain?`Abrir Channel Brain v${brain.version}`:'Criar Channel Brain'}</span></button></article>})}</section>
  {!visible.length&&<div className="empty-state"><span className="empty-icon"><FolderKanban size={30}/></span><h3>Nenhum canal neste estágio</h3><p>Cadastre um canal ou transforme uma perspectiva do radar em uma iniciativa editorial.</p></div>}
  <section className="channel-source-panel"><div className="panel-heading"><Sparkles size={20}/><h2>Perspectivas prontas para virar canal</h2></div><p>Estas propostas vêm da anatomia de referências do radar. Escolha apenas as que fazem sentido para seu portfólio.</p><div className="source-opportunities">{opportunities.slice(0,5).map(({channel,opportunity})=><article key={opportunity.id}><span className="tag orange">{channel.niche}</span><h3>{opportunity.name}</h3><p>{opportunity.lens}</p><button className="button subtle small" onClick={()=>addFromOpportunity(channel,opportunity)}><Layers3 size={15}/>Adicionar ao portfólio</button></article>)}</div>{demo&&<p className="demo-source-note">As propostas desta demonstração são fictícias e servem apenas para explorar o fluxo da plataforma.</p>}</section>
 </>;
}
